# Vite が設定読み込み時に生成する一時ファイル / 依存キャッシュ
app/*.timestamp-*.mjs
app/.vite/

# 依存・ビルド中間物（app/.gitignore でも個別に無視）
node_modules/
dist/

# 環境変数（実値はコミットしない。テンプレートは .env.example を使用）
app/.env
.env

# OS / エディタ
.DS_Store
*.log
